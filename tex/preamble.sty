% Packages used
\usepackage[spanish,es-nosectiondot]{babel} % fechas en español etc
\usepackage[utf8]{inputenc} % acentos
\usepackage[margin=1in]{geometry} %?
\usepackage{amsmath,amsthm,amssymb} % paquetes de mate
\usepackage{physics}    % para hacer derivadas + fácil
\usepackage{enumitem} % me deja editar los enumerate
\usepackage{listings} % insertar código
\usepackage{cancel}  % tachar en entornos matemáticos
\usepackage{graphics,graphicx,float} % manejo de figuras
\usepackage[small]{titlesec} % manejo de títulos en SECCIONES
\usepackage{titletoc} % manejo de títulos en la tabla de contenidos
\usepackage[dvipsnames]{xcolor} % usar colores
\usepackage{colortbl} % no se porque deje esto acá? 
\usepackage{tikz} % PARA HACER DIAGRAMAS!
\usepackage{titling} % para editar en detalle titulos del doc
% \usepackage[superscript, biblabel, nomove]{cite} %bibliografia
\usepackage{calc} % auxiliar para definir algunas cosillas
\usepackage{accents} % usar el ñuflo en entorno matemático
\usepackage{wrapfig} % permite poner figura al costado del texto
\usepackage{pgfplots} % para hacer gráficos de funciones
%------------%-----------
%% Defino colores que uso, son paletas de VERDE
% con gris como neutro,
% si los quieren cambiar por otras paletas
% aca hay mas colores: http://latexcolor.com/
% Si los quieren sacar Crtl+F reemplazan todo por black

\definecolor{oscuro}{rgb}{0.0, 0.2, 0.13}           %darkgreen
\definecolor{medio}{rgb}{0.05, 0.5, 0.06}           %dartmouthgreen
\definecolor{claro}{rgb}{0.56, 0.74, 0.56}          %darkseagreen
\definecolor{muyclaro}{rgb}{0.67, 0.88, 0.69}       %celadon
\definecolor{neutro}{HTML}{443b3e}                  % Gris oscuro
%\definecolor{neutroclaro}{HTML}{b0bea9}             % Gris claro
%------------
%%%para que los hipervínculos no tengan la cajita, sean de colores y tengan un subrayado (en el output del pdf no acá):
%%%%%% Descomentar esta sección si no lo querés.
\usepackage[pdfmenubar]{hyperref}
\hypersetup{
 pdfborderstyle={/S/U/W .5}, %subrayado cuando imprima
 urlbordercolor= oscuro, %subrayado en links externos
 linkbordercolor= white,
 citebordercolor= white,
 filebordercolor= white
 }
\newcommand{\reffont}{\color{medio}}% References will be \small
\AtBeginDocument{%
  \LetLtxMacro\oldref\ref% Capture \ref in \oldref
  \renewcommand{\ref}[1]{% Update \ref to use...
  {\reffont\oldref{#1}}}% ...\reffont
}
%------------
% formateo los captions de FIGURAS y TABLAS
%% comentar para sacarlo
\usepackage[justification=centering, width=1.1\textwidth]{caption} 
\captionsetup[figure]{labelfont={bf},name={Figura},labelsep=period}
\captionsetup[table]{labelfont={bf},name={Tabla}, labelsep=period}
%------------
% formateo los items
%% para que tengan de figurita una linea
% y los enumerate para que sean a) b) .. en negrita

%%comentar para sacarlo
\renewcommand\labelitemi{$-$} % los itemize tienen este item ahora
\setlist[enumerate,1]{label=\textbf{\alph*)}} % los enumerate usan este estilo
%------------


%------------
%% Redefino los boxes en el mathmode para que queden de colores.
%% comentar si no se quiere
\makeatletter  % para poder usar los arroba en codigo!
\renewcommand{\boxed}[1]{\fcolorbox{muyclaro}{muyclaro}{\m@th$\displaystyle#1$}}
\makeatother  % para desactivar los arroba

%------------
% Paquetes extra de TIKZ
% Si se usa TIKZ es preferible dejarlos
\pgfplotsset{compat=1.17}  % lo puse para sacar un warning

\usetikzlibrary{arrows.meta}  % mejora las flechas de los vectores
\usetikzlibrary{babel} % me deja usar acentos
\usetikzlibrary{patterns.meta} % hace rellenos en patron



%------------
%%%% DEFINO ESTILO DE CODIGO DE PYTHON !
%%% Super random, si no se quiere usar  comentarlo.
%New colors defined below
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{codeblue}{rgb}{0.2, 0.2, 0.6}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\definecolor{codered}{rgb}{0.8, 0.0, 0.0}
%Code listing style named "mystyle"
\lstdefinestyle{mystyle}{
  backgroundcolor=\color{backcolour},   commentstyle=\color{codegreen},
  keywordstyle=\color{magenta},
  numberstyle=\tiny\color{codegray},
  stringstyle=\color{codepurple},
  basicstyle=\ttfamily\footnotesize,
  breakatwhitespace=false,         
  breaklines=true,                 
  captionpos=b,                    
  keepspaces=true,                 
  numbers=left,                    
  numbersep=5pt,                  
  showspaces=false,                
  showstringspaces=false,
  showtabs=false,                  
  tabsize=2,
  language=python,
%  emph={}  %%% esto es mejor ingresarlo manualmente en cada llamada
  emphstyle=\color{codeblue},
  morekeywords={True},
  postbreak=\mbox{\textcolor{codered}{$\hookrightarrow$}\space},
  morekeywords=[1]{,as,assert,nonlocal,with,yield,self,True,False,None,} % Python builtin
  morekeywords=[2]{,__init__,__add__,__mul__,__div__,__sub__,__call__,__getitem__,__setitem__,__eq__,__ne__,__nonzero__,__rmul__,__radd__,__repr__,__str__,__get__,__truediv__,__pow__,__name__,__future__,__all__,}, % magic methods
  morekeywords=[3]{,object,type,isinstance,copy,deepcopy,zip,enumerate,reversed,list,set,len,dict,tuple,range,xrange,append,execfile,real,imag,reduce,str,repr,}, % common functions
  morekeywords=[4]{,Exception,NameError,IndexError,SyntaxError,TypeError,ValueError,OverflowError,ZeroDivisionError,}, % errors
  morekeywords=[5]{,ode,fsolve,sqrt,exp,sin,cos,arctan,arctan2,arccos,pi, array,norm,solve,dot,arange,isscalar,max,sum,flatten,shape,reshape,find,any,all,abs,plot,linspace,legend,quad,polyval,polyfit,hstack,concatenate,vstack,column_stack,empty,zeros,ones,rand,vander,grid,pcolor,eig,eigs,eigvals,svd,qr,tan,det,logspace,roll,min,mean,cumsum,cumprod,diff,vectorize,lstsq,cla,eye,xlabel,ylabel,squeeze,np,}, % numpy / math
  classoffset=1,
  morekeywords={, fits,util,miles,log_rebin,io,ppxf,}, 
  keywordstyle=\color{codeblue},
}

% queda por defecto:
\lstset{style=mystyle}

%\captionsetup[lstlisting]{singlelinecheck=false, margin=0pt,labelsep=period,labelfont={bf},name=Código}

\renewcommand\lstlistingname{Código}
\renewcommand\lstlistingnamestyle{\bfseries}





\usepackage{wrapfig}


\usepackage[square,authoryear,sectionbib]{natbib}
\usepackage{aas_macros}
\setcitestyle{authoryear, open={[},close={]}}
% \usepackage{apacite}
\usepackage{doi}

% \AtBeginDocument{\bibliography{biblio}}

% \AtBeginDocument{\bibliographystyle{bibstyle2}}